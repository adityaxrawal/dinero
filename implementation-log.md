# Implementation Log

Append-only. One line per task, appended only once the task is genuinely complete (code written, tested, committed). An unlogged task reads as `not started` to the next session by design.

| Task ID | Area | Status | Timestamp | Note |
|---|---|---|---|---|
| TASK-SETUP-001 | SETUP | done | 2026-07-14T00:00:00Z | Pre-existing scaffold matched spec; fixed `productName` "dinero-app" → "Dinero". |
| TASK-SETUP-002 | SETUP | done | 2026-07-14T00:05:00Z | Tightened CSP to spec: dropped direct WebView access to Google/GitHub domains, fixed licensing origin to license.dinero.app. |
| TASK-SETUP-003 | SETUP | done | 2026-07-14T00:10:00Z | No code change — Tauri v2's capability model already disables generic shell/fs/http for the WebView. |
| TASK-SETUP-004 | SETUP | done | 2026-07-14T00:12:00Z | Fixed window config: title, width, minWidth, transparent; added explicit resizable/decorations/center. |
| TASK-SETUP-005 | SETUP | done | 2026-07-14T00:14:00Z | Dependency set matches spec except sqlx (flagged for TASK-DB-001/002 — existing code uses rusqlite_migration). |
| TASK-SETUP-006 | SETUP | done | 2026-07-14T00:20:00Z | Implemented from scratch: src-tauri/src/startup.rs RAM check, llm_eligible state, system_warning event. |
| TASK-SETUP-007 | SETUP | done | 2026-07-14T00:25:00Z | tsconfig: ES2022, exactOptionalPropertyTypes, 4 missing path aliases (+ vite.config.ts mirror). Fixed 2 latent tsc errors so `tsc --noEmit` passes clean. |
| TASK-SETUP-008 | SETUP | done | 2026-07-14T00:30:00Z | no-explicit-any -> error, .prettierrc corrected to spec. FLAGGED: pnpm lint now reports 76 errors (pre-existing `any` usage in Area 9 pages, not fixed here — deferred to owning TASK-FE-* tasks). |
| TASK-SETUP-009 | SETUP | done | 2026-07-14T00:32:00Z | rust.yml already matches/exceeds spec. No change. |
| TASK-SETUP-010 | SETUP | done | 2026-07-14T00:33:00Z | react.yml already matches/exceeds spec. No change. FLAGGED: ESLint step will fail until TASK-SETUP-008's any-errors are fixed. |
| TASK-SETUP-011 | SETUP | done | 2026-07-14T00:34:00Z | benchmark.yml already matches spec; phase10_quality_gates_tests.rs verified to implement all 3 Doc 34 §10.2 thresholds. No change. Placeholder repo name flagged, not fixable now. |
| TASK-SETUP-012 | SETUP | done | 2026-07-14T00:45:00Z | Added Parse/Io/Internal/Validation variants + code() mapping; fixed real bug: Serialize emitted bare string, frontend expected {code,message} — every error was silently becoming UNKNOWN_ERROR. No From<AppError> impl needed (Tauri's own blanket Serialize->InvokeError impl covers it; writing one would conflict). |
| TASK-SETUP-013 | SETUP | done | 2026-07-14T01:00:00Z | Added ipc::with_panic_boundary (async-safe tokio::spawn/JoinError, not sync catch_unwind — not yet wired into existing commands, flagged for TASK-API-001). Added useIpcInvoke/useIpcListen hooks + src/types/ipc.ts (new, additive). |
| TASK-SETUP-014 | SETUP | done | 2026-07-14T01:05:00Z | PR/issue templates already matched spec exactly. No change. |
| TASK-SETUP-015 | SETUP | done | 2026-07-14T01:10:00Z | Created docs/dev-setup.md (OAuth dev setup, DB reset, privacy invariants); corrected README.md prerequisites (Node/pnpm/macOS versions, Xcode CLT, Keychain note). |
| TASK-DB-001 | DB | done | 2026-07-14T01:30:00Z | Fixed real Argon2 salt bug (hardcoded constant, not hw_uuid-derived); added auto_vacuum=INCREMENTAL + guarded VACUUM; renamed data.db->finance.db (4 sites). FLAGGED: cargo clippy/fmt both fail pre-existingly, codebase-wide (unrelated to this task) — rust.yml CI has likely never passed. |
| TASK-DB-002 | DB | done | 2026-07-14T03:00:00Z | User-directed: migrated rusqlite_migration -> sqlx::migrate! (19 migrations converted to timestamped .sql files). Verified SQLCipher compat empirically (shared libsqlite3-sys). Converted ~20 test files' setup_db() helpers from open_in_memory() to temp-file async migration (sqlx can't reach in-memory rusqlite connections). 313 tests pass, same 4 pre-existing unrelated failures. |
| TASK-DB-003 | DB | done | 2026-07-14T03:10:00Z | Table/CRUD/tests already matched spec. Fixed real bug: seed defaults were timezone='UTC', limit_thresholds='[]' instead of spec's 'Asia/Kolkata'/[80,90,100]. |
| TASK-DB-004 | DB | done | 2026-07-14T03:20:00Z | Table/CRUD/FK-violation tests already matched spec. Added missing UNIQUE constraint on email_address (new migration 020) - same Gmail account could previously be connected twice. |
| TASK-DB-005 | DB | done | 2026-07-14T03:30:00Z | Table/CRUD/get_or_create_instrument/all acceptance tests already matched spec exactly. Added missing idx_instruments_type/idx_instruments_issuer indexes (new migration 021) per Document 18 §4.2/§6. |
| TASK-DB-006 | DB | done | 2026-07-14T03:45:00Z | Table/CRUD/8-of-9 acceptance tests already matched spec. Added 4 missing indexes (new migration 022) - dedup lookups were full table scans without them. Added missing updated_at trigger test. |
| TASK-DB-007 | DB | done | 2026-07-14T04:00:00Z | Fixed real "wrong table" deviation: FTS table was named transactionsfts with shadow generated columns instead of transactions_fts with real columns per Document 18 §4.3a. Verified empirically the doc's simpler DDL works fine (no real constraint being worked around). New migration 023 corrects it; updated search_transactions() call site. |
| TASK-DB-008 | DB | done | 2026-07-14T04:30:00Z | Added missing source_pipeline CHECK (migration 024). PAUSED and asked user: adding it surfaced a real production bug in historical_scan.rs (Area 4) writing source_pipeline="historical_scan", never valid. User said fix it -> corrected to "gmail_transaction", plus ~20 test-fixture placeholder values across 4 files. |
| TASK-DB-009 | DB | done | 2026-07-14T05:00:00Z | Added missing source_type column + CHECK, parse_status CHECK, billing_period index (migration 025). Fixed update()/soft_delete() to detect not-found. Added 5 missing acceptance-criteria tests. FLAGGED for later: audit_log.rs's comment claims consent events belong in audit_log, but Document 18 §4.21a says otherwise (dedicated consent_events table) -- that's TASK-AUTH-003's scope (Area 3), not fixed here. |
| TASK-DB-010 | DB | done | 2026-07-14T05:10:00Z | Table/CRUD already matched spec (cascade-delete-only, already tested). Added 2 missing indexes (migration 026) per Document 18 §6. |
| TASK-DB-011 | DB | done | 2026-07-14T05:30:00Z | Immutability trigger + test already existed. Added CHECK constraints on decision/review_status (migration 027) + 2 indexes. Fixed real bug: reconciliation/audit.rs used "unreviewed"/"needs_review" instead of Document 18's "not_required"/"pending_review" -- zero downstream readers, fixed without re-asking (same pattern as DB-008, lower risk). Fixed ~6 test-fixture placeholder decision values. |
| TASK-DB-012 | DB | done | 2026-07-14T06:15:00Z | User-directed: three-way schema conflict (code vs Doc 18 vs Doc 30) on reconciliation_clusters/_members -- user chose "rewrite to match Document 18." Migration 028 recreates both tables to Doc 18 §4.6/§4.6a exact shape (cluster_status 4-state enum, member_role discriminator including 'incoming' for the triggering observation). Rewrote cluster.rs's create/resolve logic + fixed a real latent analytics bug the new status model exposed: exclusion query previously hid a transaction from spend totals forever once ambiguous, even after resolution -- now scoped to cluster_status='open' only. Updated alert_worker.rs and commands/data.rs call sites. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-012b | DB | done | 2026-07-14T06:20:00Z | Follow-up: added missing reconciliation_clusters(cluster_status) index per Document 18 §6, missed in the DB-012 migration. |
| TASK-DB-013 | DB | done | 2026-07-14T06:45:00Z | merchants/merchant_aliases predated Doc 18 (category_id/logo_url/website vs missing normalized_name/source; alias/merchant_id vs alias_raw+alias_normalized/merchant_entity_id). Migration 030 drops+recreates to Doc 18 §4.10/§4.11 exact shape + reseed. Rewrote db/merchants.rs. Real bug found: post_processing.rs derived transaction category from merchants.category_id (a column Doc 18 doesn't spec) -- removed that derivation, category assignment now falls through fully to the existing keyword-heuristic fallback (already verified working via test_category_assigned_from_merchant_entity). Fixed 2 raw-SQL test fixtures. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-014 | DB | done | 2026-07-14T07:10:00Z | Table/CRUD/seed/all 4 named acceptance areas already existed and passed. Fixed real deviation from Doc 18 §4.9: color/icon/is_system(bool)/updated_at -> source_type enum (system/user/mcc_mapped)/mcc_code/monthly_budget_minor. Migration 031 rewrites table, carrying seed hierarchy forward. Hit and fixed a real SQLite self-referencing-FK gotcha (parent_id -> categories.id): the standard create-new/drop-old/rename pattern orphaned the new table's rows on DROP, breaking DB init for the whole suite -- fixed via a plain temp-table copy + parent-before-child insert ordering. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-015 | DB | done | 2026-07-14T07:20:00Z | tags/transaction_tags already matched Doc 18 §4.9a/§4.9b almost exactly (CASCADE/RESTRICT already correct, tag CRUD tests already covered). Fixed one real deviation: color -> color_hex (migration 032), one production call site (commands/mod.rs) + tests updated. Flagged, not changed: transaction_tags uses composite PK (transaction_id, tag_id) vs Doc 18's surrogate id PK -- functionally identical, same class as prior Keychain/FK-naming decisions. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-016 | DB | done | 2026-07-14T07:30:00Z | pdf_passwords/pattern_rules/feedback_log already matched Doc 18 §4.12/§4.13/§4.19 exactly; Layer 1 state machine, AES-256-GCM password encryption, and source_pipeline validation all already implemented and tested. Only gap: 3 missing indexes per Doc 18 §6, added via migration 033. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-017 | DB | done | 2026-07-14T07:35:00Z | processing_checkpoints/unprocessed_statements/unassigned_transactions already matched Doc 18 §4.15/§4.16/§4.17 exactly incl. UNIQUE(job_type,job_key); both named acceptance tests (UPSERT idempotency, retry lifecycle) already existed and pass. Only gap: 2 missing status indexes per Doc 18 §6, added via migration 034. 319 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-018 | DB | done | 2026-07-14T08:00:00Z | recurring_payments/sessions/audit_log/license_state already matched Doc 18 §4.14/§4.20/§4.21/§4.22 field-for-field. Added 5 missing indexes + billing_interval_cached CHECK (migration 035). Real feature gap implemented: audit_log's prev_hash/row_hash tamper-evidence hash chain (Doc 18 §4.21, not mentioned in Doc 30's task text) -- insert() now computes SHA256 chain automatically, new verify_chain() detects out-of-band tampering the immutability trigger alone can't catch (direct file access bypasses it). Caught+fixed a real precision-mismatch bug in the hash formula via the test itself (SQLite DATETIME truncates to whole seconds). 2 new tests. 321 passed, same 4 pre-existing unrelated failures. |
| TASK-DB-019 | DB | done | 2026-07-14T08:45:00Z | Retention/archival steps already existed (db/retention.rs) and wired into a daily loop. Added the 3 missing steps in new db/maintenance.rs: integrity_check (emits db_corrupted + audit_log on failure), incremental_vacuum, size warning past 2GB -- wired into the same loop. 3 new tests. |
| TASK-DB-020 | DB | done | 2026-07-14T08:45:00Z | Daily VACUUM INTO backup mechanism already existed, deliberately following Doc 28 (single rolling file) over Doc 30's own "retain 7" text -- a pre-existing, reasoned decision, not re-litigated. Added missing db_backup_completed audit_log entry + event emission on success. |
| TASK-DB-021 | DB | done | 2026-07-14T08:45:00Z | Rekey mechanism (crypto::try_migrate_hardware_uuid) already existed, tested, and wired into init_db's key-mismatch path. Added missing "Database migrated to new Mac" toast via new hw_uuid_marker_indicates_migration() peek + db_hardware_migrated event. Flagged not fixed: marker uses a plaintext file not Keychain (pre-existing reasoned deviation); license re-activation deferred to Area 3 (needs real licensing-backend calls). 1 new test. |
| TASK-DB-022 | DB | done | 2026-07-14T08:45:00Z | Found and fixed a real live instance of the exact anti-pattern this task targets: auth_google_start accepted profile_id directly from the frontend IPC call. New db/scoping.rs (LOCAL_PROFILE_ID=1); removed profile_id from the IPC signature entirely, resolved internally instead. Frontend (src/lib/ipc.ts) already always passed the hardcoded value -- updated to match. 2 new tests (test_user_data_isolation equivalent). Combined with DB-019/020/021 in one commit since all four share the same lib.rs daily-loop/init_db code region. 327 passed total, same 4 pre-existing unrelated failures. |
| TASK-DB-023 | DB | done | 2026-07-14T09:00:00Z | Nothing existed. New db/batch_writer.rs: BatchWriter groups writes into bounded BEGIN IMMEDIATE...COMMIT transactions (max 20 rows / 500ms), retries SQLITE_BUSY with exponential backoff (max 3), rolls back the whole batch on any other error. Synchronous by construction -- no await in the type, so it's structurally safe to use from inside conn.interact(). Not yet wired into the 3 named consumers (historical scan/poll worker/statement parser) -- flagged as deferred to those tasks' owning Areas (4/6/7), consistent with this task's own acceptance criteria deferring validation to the not-yet-built TASK-QA-002. 5 new tests. 332 passed, same 4 pre-existing unrelated failures. |

**Area 2 (Database) complete: all 23 tasks (TASK-DB-001 through TASK-DB-023) done.**
| TASK-AUTH-001 | AUTH | done | 2026-07-14T09:30:00Z | PKCE/system-browser/loopback/state-validation/Keychain-only storage all already correct in ingestion/oauth.rs (Doc 30 names auth/google_oauth.rs -- path difference only). Both acceptance tests already existed and passed. Real gap: no 5-minute timeout on the callback wait -- added wait_for_oauth_callback() using tiny_http's recv_timeout, times out with exactly "oauth_timeout" matching AppError::Auth's expected value. 1 new test. 333 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-002 | AUTH | done | 2026-07-14T10:00:00Z | Onboarding.tsx step 3 already implements the must-complete, unbypassable disclosure. Fixed 2 real content gaps: Licensing Backend disclosure didn't explicitly state it never receives financial data; CTA button read "Connect & Finish" instead of Doc 30's exact "I Understand, Continue to Google" -- renamed, updated e2e locator. |
| TASK-AUTH-003 | AUTH | done | 2026-07-14T10:00:00Z | Resolved the consent_events-vs-audit_log conflict flagged (not fixed) at TASK-DB-009 -- Doc 18 §4.21a + Doc 19 §5.6 both confirm a dedicated table. New migration 036 + new src-tauri/src/auth/consent.rs (first real auth/ module). Migrated all 4 real call sites (checked Settings.tsx's Consent History panel first -- it advertises all 3 categories, so all had to move together). Renamed IPC read command to auth_get_consent_history (Doc 19 exact name). Real behavioral fix: removed backend's redundant/wrongly-timed internal consent recording, relying solely on the frontend's already-correctly-timed pre-flight recording; renamed its event type to gmail_oauth_consent with real verbatim disclosure text. 4 new tests. 337 passed total, same 4 pre-existing unrelated failures. |
| TASK-AUTH-004 | AUTH | done | 2026-07-14T10:30:00Z | Proactive refresh + degradation handling already solid. Added HTTP 401 reactive retry-once to polling.rs's history loop (the highest-traffic Gmail call site) via new force_refresh_access_token() -- flagged as a deliberately partial implementation given the task's S effort rating (GmailClient's other methods / historical_scan.rs still proactive-only). Fixed 2 real gaps: handle_invalid_history_id now writes the history_checkpoint_reset audit_log entry Doc 30 names; refresh-failure path no longer logs/persists the raw OAuth error text (a literal violation of this task's own "never expose... to logs" requirement) -- new classify_refresh_error() reduces it to a fixed safe vocabulary everywhere. 1 new test. 338 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-005 | AUTH | done | 2026-07-14T11:00:00Z | Nothing existed beyond bare sessions table CRUD -- implemented from scratch. New src-tauri/src/auth/session.rs: SessionState (Tauri-managed in-memory, session_id never sent to React), compute_device_fingerprint (SHA-256(hw_uuid+bundle_id) per spec, deliberately not reusing licensing::device's different UUID-v5 derivation), ensure_active_session (idempotent), logout (revoke not delete). Wired into lib.rs startup + new auth_logout IPC command. Deferred correctly: blanket re-auth enforcement across all commands is TASK-AUTH-008's explicit job. 4 new tests. 342 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-006 | AUTH | done | 2026-07-14T11:20:00Z | auth_google_disconnect already handled Keychain deletion + per-account update correctly. Fixed 3 real gaps: never called Google's revoke endpoint on single-account disconnect (extracted revoke_single_account_with_google, now shared with the full-wipe path); account_status was 'INACTIVE' not Doc 30's 'disconnected' (checked all 3 read sites first, zero-risk rename); audit_log action was 'disconnect_gmail' not 'gmail_revoked'; never withdrew the gmail_oauth_consent event (TASK-AUTH-003's withdraw_consent_event now wired in). Judged the "per-account CancellationToken" requirement unnecessary given the actual single-shared-loop polling architecture already achieves the same effect via the status flip. 1 new test. 343 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-007 | AUTH | done | 2026-07-14T11:45:00Z | No src-tauri/tests/ directory existed; same invariants were covered by weak source-grep unit tests under different names (e.g. checking only for literal substrings like "pub token: String"). New src-tauri/tests/secrets_audit.rs -- a real integration suite against the public API, all 5 of Doc 30's exact acceptance test names, each strengthened (exhaustive struct literal as a compile-time drift guard, real byte-length AES-GCM structure check, real recursive filesystem scan for the derived key, real PRAGMA table_info schema-wide scan). Real false positive caught+fixed by the schema scan itself: network_activity_log.secret_fields_masked is a legitimate metadata column, not a secret -- added an explicit exemption. Existing weaker tests left in place (different testing boundary, not true duplication). 6 new integration tests. 343 lib tests unchanged + 6 new, same 4 pre-existing unrelated failures. |
| TASK-AUTH-008 | AUTH | done | 2026-07-14T12:15:00Z | Codebase-wide scan found zero remaining profile_id/user_id-carrying IPC commands (the one instance was already fixed at TASK-DB-022). New src-tauri/src/ipc/middleware.rs (require_active_session) wired into auth_google_disconnect/license_activate/license_deactivate -- the specific Gmail/licensing commands Doc 30 names, following the same "primitive now, full ~53-handler retrofit later" pattern ipc::with_panic_boundary already established. Real conflict caught by the build: license_deactivate is also called internally (bypassing Tauri IPC) by the Reset-App-Data wipe flow, which must deactivate unconditionally per Doc 28 §4.4's Local Wipe Priority -- extracted deactivate_license_internal (session-check-free) so the wipe flow isn't blocked by the new gate. New src-tauri/tests/tenant_isolation.rs implements Doc 30's exact test_data_isolation_ipc_parameter_attack (codebase-wide signature scan) + a behavioral no-default-fallback test. 5 new tests total. 345 lib + 6 secrets_audit + 3 tenant_isolation passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-009 | AUTH | done | 2026-07-14T12:45:00Z | Schema/enum/write-gate/background worker all already solid. Real gap: no actual transition-legality enforcement anywhere -- every local status change was a raw unconditional UPDATE. New licensing/state_machine.rs: is_legal_transition() graph matching Doc 30's exact list (+ AnonymousEval->Locked, preserving existing clock-skew-lock behavior for never-activated installs, not in Doc 30's text but not newly rejected either). Routed state.rs's transition_to_locked and worker.rs's Active->Grace through it; deliberately left the "trust a server-verified JWT" writes (successful validation/activation) untouched since those are a different kind of write. Renamed a same-named old test (didn't test rejection at all) to avoid confusion with the new real acceptance test. 4 new tests including the exact-named test_license_state_machine_transitions (walks every legal edge, asserts Doc 30's own named illegal example is rejected). 349 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-010 | AUTH | done | 2026-07-14T13:00:00Z | Background worker already implements nearly everything: interval loop firing immediately at cold-start, JWT verification before trust, network-failure->GRACE, clock-skew detection with the exact 1-hour grace named. ValidateRequest already device_id-only, correctly per Doc 19 §14.2/§14.4's already-established no-license-key design (Doc 30's "license_key/email" text is a generic paraphrase, not a real gap). 6-hour interval vs Doc 30's "24 hours" already resolved via Doc 40 §7, not re-litigated. Real gap flagged not implemented: no macOS sleep/wake ("resume") event listener exists anywhere -- would need native NSWorkspace FFI bindings, disproportionate to Effort M; narrow impact (max 6h staleness after a long sleep, well within the 7-day grace). Added the acceptance-named test for the validate call specifically (existing test under that name only covered activate). 350 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-011 | AUTH | done | 2026-07-14T13:20:00Z | activate/deactivate/refresh all already existed, JWT-verified, session-gated (except refresh -- added for consistency, all 3 share the same constraints per Doc 30's own text). Real gap: DEVICE_ALREADY_BOUND (Doc 19 §14.2/§4's documented code) was never surfaced -- error_for_status_ref() discards the response body entirely, throwing away the actual error code in favor of a generic failure message. Fixed: activate() now parses the {code,message,details} error shape on failure and propagates the code; commands.rs catches DEVICE_ALREADY_BOUND specifically and returns clear deactivate-elsewhere-first guidance. 1 new test. 351 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-012 | AUTH | done | 2026-07-14T13:30:00Z | Already fully implemented and well-tested -- embedded RSA-256 key via include_str!, signature+expiry validation, comprehensive test suite (wrong-key/tampered/expired rejection). No code changes needed. Gap flagged, deliberately not guessed at: no issuer (iss claim) validation exists, but no document anywhere defines what this JWT's issuer claim would even contain -- unlike prior tasks' license_key/email non-issues, there's no established resolution to point to here; guessing risks either a no-op or rejecting every real JWT the actual backend issues. |
| TASK-AUTH-013 | AUTH | done | 2026-07-14T13:50:00Z | Nearly everything already existed correctly (audit-before-destruction, Gmail revoke, license deactivate, Keychain clear, restart-to-onboarding). Stale comment claimed the two-step typed-confirm UI was deferred to a separate ticket -- it already exists in full in Settings.tsx with the exact quoted phrase "DELETE MY DATA"; corrected the comment. Real gap: pre-migration .bak files (written directly to app_dir, not the backups/ subdirectory) were never swept by the wipe, silently surviving a "delete all my data" request. Second gap: no final account_deletion_completed record at all -- writing it to audit_log would be self-defeating since that table lives inside the file being deleted next; logged via tracing instead, which actually persists. Extracted delete_finance_db_and_all_backups() with 1 new test proving every target file category is removed while an unrelated file survives. 352 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-014 | AUTH | done | 2026-07-14T14:10:00Z | Nothing existed -- new security/incident_response.rs built from scratch, reusing existing response mechanisms (session revoke, Gmail disconnect, audit logging) from earlier tasks. IncidentMonitor: in-memory-only per-trigger counters, fires once per threshold crossing then resets. Wired into 2 of 4 named triggers with real existing hooks: integrity_check failures (fires on first occurrence) and the OAuth degradation path's invalid-token case (threshold 3). Flagged, not wired: "refresh spikes" (no existing rate-tracking to hook into) and "audit-log write failure" (circular -- recording that audit_log::insert failed by calling audit_log::insert again doesn't work if the table is genuinely broken). 3 new tests, one of which caught a miscounted assertion in my own draft via the test run itself. 355 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-015 | AUTH | done | 2026-07-14T14:35:00Z | Bundle generation already existed and was already largely correct (manifest, table counts, redacted error logs/crash reports, consent recording). 4 real gaps fixed: pipeline health metrics weren't in the bundle at all (now reuses do_get_debug_metrics); no audit-log content at all, only counts (added collect_redacted_audit_log, category fields verbatim + after_json redacted); the redaction regex's 6+-digit-only number pattern missed real transaction amounts like "₹499.00" (added explicit card-number and rupee-amount patterns matching the acceptance criteria's exact three named patterns); "blocking export on any match" had no runtime gate at all, only whatever redact() happened to catch (added scan_for_pii as a real pre-finalize gate). 4 new tests including a literal implementation of the acceptance criterion: seeds real PII into a real DB, generates a real bundle, unzips every actual file written to disk, and scans the real bytes for zero matches. 359 passed, same 4 pre-existing unrelated failures. |
| TASK-AUTH-016 | AUTH | done | 2026-07-14T14:50:00Z | No checklist existed. New docs/casa-verification-checklist.md -- every claim checked against actual code (token-Keychain-only storage per TASK-AUTH-007's tests, exact-scope consent matching per the actual add_scope call site, CSP's real connect-src allowlist, encryption-at-rest). No code changes needed -- the two properties this task cares about were already independently verified true earlier this session. Honestly flagged rather than overclaimed: Statement-Only Mode contingency (TASK-FE-017, Area 9, not reached) is only partially built -- new-user skip-Gmail path exists, but the already-connected-user-loses-access case does not. |

**Area 3 (Authentication, Sessions, Tenant Isolation, Licensing Gate) complete: all 16 tasks (TASK-AUTH-001 through TASK-AUTH-016) done.**
| TASK-GMAIL-001 | GMAIL | done | 2026-07-14T15:10:00Z | Poll loop/checkpoint-after-full-cycle/401-refresh/HISTORY_ID_TOO_OLD already correct. Fixed real gap: 429/5xx backoff had no 60s cap and no jitter, and gave up after 3 retries (never near 60s) — added `next_backoff`/`jittered`, raised max_retries to 8. Renamed 1 test to spec's exact name, added missing `test_backoff_increases_on_429`. 360 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-002 | GMAIL | done | 2026-07-14T15:30:00Z | Metadata-first Gate 1 shape already correct. Fixed 3 real gaps: `pdf_attachments` only captured (attachment_id, filename), missing spec's mimeType/size — new `PdfAttachmentMeta` struct populates all 4 from already-parsed data; no 50-concurrent quota semaphore existed for full-message fetches — added `full_fetch_semaphore()`; `GmailClient` had no injectable base URL (unlike `LicensingClient`), making it untestable — added `new_with_base_url`. 2 new tests (mockito zero-hit proof, semaphore concurrency proof). 362 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-003 | GMAIL | done | 2026-07-14T15:45:00Z | All 4 acceptance behaviors (plaintext-preferred, script/style-stripped-span-preserved, quote-stripped, signature-stripped) already correct. Removed 1 line of dead code (unreachable trim comparison). Renamed 1 test, split 1 combined test into the 2 exact-named acceptance tests. 363 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-004 | GMAIL | done | 2026-07-14T16:00:00Z | Exact-match/subdomain-nesting/display-name spoof checks already correct; 202-entry registry already covers major Indian banks. Fixed 2 real gaps: hand-rolled Levenshtein replaced with already-a-dependency `strsim::levenshtein` (spec named this explicitly); homoglyph/Unicode-look-alike detection was entirely missing — added `normalize_confusables()` + punycode-label rejection. 3 new tests (typo-squat split out, homoglyph, punycode). 366 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-005 | GMAIL | done | 2026-07-14T16:15:00Z | OTP/KYC hard-reject and statement-priority already correct. Fixed real gap: marketing/reminder keywords hard-rejected unconditionally, with no check for a genuine settled-transaction signal also present — a real "Cashback Offer: you spent Rs.499" email would've been silently dropped as Marketing. Added `has_amount_pattern`/`has_transaction_verb`-gated exceptions matching Doc 30's exact text. 4 new tests (2 exact-named splits, 2 regression tests for the fixed nuance). 370 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-006 | GMAIL | done | 2026-07-14T16:25:00Z | Gate 3 pass condition already correct. Fixed real gap: rejection always logged bare "gate3_failed", losing the missing_amount/missing_counterparty distinction Doc 30 requires — added `gate3_failure_reason()`. Added missing 3rd acceptance test `test_gate3_fails_merchant_only`. 371 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-007 | GMAIL | done | 2026-07-14T16:45:00Z | Checkpoint/resume/pagination/events already correct. Fixed 3 real gaps: concurrent-scan rejection was never enforced (logged a warning and started a second scan anyway) — added `reject_if_scan_in_progress()`; checkpoint interval was 10 not spec's 5 — added `should_checkpoint()`/`CHECKPOINT_INTERVAL`; the semaphore-gated concurrency was structurally a no-op (spawn-1-drain-1 pattern meant effective concurrency was always 1) — rewrote as a proper prime-then-refill pipeline capped at 50. 2 new tests, 1 renamed. 373 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-008 | GMAIL | done | 2026-07-15T09:00:00Z | Routing itself (in-memory Vec<u8>, channel handoff never a file path, sequential multi-attachment iteration) already correct as a side effect of TASK-GMAIL-002. Only gap: the one named acceptance test, `test_pdf_bytes_never_touch_filesystem`, didn't exist. Added via mockito; first attempt over-asserted on filenames (broke on init_db's own legitimate backup/marker files) — rewrote to scan file contents for the PDF byte sequence instead. 374 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-009 | GMAIL | done | 2026-07-15T09:20:00Z | Independent per-account checkpoints and catch-and-continue loop shape already correct (already-built multi-account infra, matching the note that TASK-RD-002 is stale). Both named acceptance tests were missing. Added base_url DI to poll_all_accounts/poll_single_account (mirroring GMAIL-002) to make it testable at all. `test_independent_checkpoint_per_account` is a fast direct DB test. `test_one_account_failure_does_not_block_others` needed a capturing tracing layer to prove isolation without touching real Keychain (poll_single_account's first step) or adding new IncidentMonitor introspection API — verified non-flaky over 5 runs. 376 passed, same 4 pre-existing unrelated failures. |
| TASK-GMAIL-010 | GMAIL | done | 2026-07-15T09:40:00Z | Nothing existed for any of the 4 named metrics. New `gmail_telemetry.rs`: `GmailTelemetry` singleton (OnceLock, same pattern as full_fetch_semaphore) with 4 counters, wired at the 429/5xx retry branch, the poll-cycle-duration wrap around poll_all_accounts, and Gate 1/2/3 rejection branches in message_processor.rs (shared by both poll and historical-scan paths, so one instrumentation point covers both). No acceptance tests named in source (telemetry-shape task) but wrote 5 anyway, including a structural no-content-fields proof via JSON string-length scanning. 381 passed, same 4 pre-existing unrelated failures. |

**Area 4 (Gmail Ingestion) complete: all 10 tasks (TASK-GMAIL-001 through TASK-GMAIL-010) done.**
| TASK-STMT-001 | STMT | done | 2026-07-15T10:30:00Z | Validation/hashing/filename-heuristic already correct. 3 user-confirmed fixes: rewrote statements_upload to Doc 19's file_bytes/results shape (was file_paths, backend disk read); made it fire-and-forget/event-driven instead of blocking on the whole pipeline (Doc 19 §3.6); added the Doc 18 §4.7 crash-recovery invariant (queued row written immediately at intake) to BOTH entry points, including already-completed Area 4 files (polling.rs/historical_scan.rs). Also fixed a real regression the crash-recovery fix would have caused: check_file_hash_duplicate checked the wrong column (source_message_id proxy-hack instead of the real file_hash column). 2 new tests for the remaining named acceptance criteria. 383 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-002 | STMT | done | 2026-07-15T10:45:00Z | Filename-first duplicate detection and pre-password ordering already correct; 2 of 3 acceptance tests already existed (surfaced during STMT-001). Fixed real gap: neither duplicate-reject site (filename-heuristic, post-metadata) ever wrote to audit_log despite Doc 30's explicit requirement — added log_duplicate_skipped_audit() at both sites with the actual detected period. 2 new tests. 385 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-003 | STMT | done | 2026-07-15T11:30:00Z | Stored-password ordering/encryption/timeout-to-pending_retry already correct (3 of 5 acceptance tests already passed). Fixed the task's entire namesake: every pdfium call ran in-process with no timeout, the exact malicious-PDF-crashes-main-process risk this task exists to close. Built a real sidecar (new src/bin/pdf_sidecar.rs standalone binary + statements/sidecar/mod.rs spawn/timeout/kill driver, user-confirmed architecture) and moved both password-unlock-check and primary text extraction into it; OCR-render call explicitly deferred to STMT-006. Fixed 1 test-naming bug. 2 new tests (isolation mechanism proven with /bin/cat and /bin/sleep, not a real PDF — verified non-flaky over 5 runs). 387 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-004 | STMT | done | 2026-07-15T11:45:00Z | Regex-based header extraction, atomic instrument resolution, and partial-metadata persistence already correct (3 of 4 acceptance tests already passed). Only gap: test_auto_create_instrument_from_statement didn't exist — added it, proving the race-tolerant reuse behavior (same key twice → same instrument, no duplicate), not just creation. 388 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-005 | STMT | done | 2026-07-15T12:15:00Z | All 7 bank/format parsers (confirmed against Doc 34 §7 directly) and row-exclusion filtering already existed and worked. Fixed real gap: broken-boundary merging existed but was never wired into any of the 7 real parsers (operated on an intermediate representation nothing produced) — a real broken row was silently dropped, not merged. Replaced with merge_broken_line_boundaries, wired into extract_rows() for all 7 at once. Renamed 3 tests to exact acceptance names, added 3 with zero prior coverage (Axis parser had never been tested at all). 391 passed, same 4 pre-existing unrelated failures (one transient unrelated flake in audit_log_tests confirmed non-reproducible). |
| TASK-STMT-006 | STMT | done | 2026-07-15T12:30:00Z | OCR threshold trigger and in-memory rendering already correct; already better than spec assumed (pipes bytes via stdin/stdout, never a temp file at all). Only gap: no RAII guard existed for the actually-at-risk resource (the spawned tesseract child process, not a file) — added TesseractChildGuard, tested by forcing a real panic via catch_unwind and confirming the process is actually dead afterward. Verified non-flaky over 5 runs. 392 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-007 | STMT | done | 2026-07-15T12:40:00Z | Two-condition rule, instrument field updates, and event emission all already correct and unusually well-tested (6 tests covering edge cases beyond the named criteria). Only gap: test_future_due_date_required's exact name was missing (rule already tested piecemeal under other names) — added it as a focused 3-case restatement. 393 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-008 | STMT | done | 2026-07-15T12:45:00Z | No code changes needed. Scoped Vec<u8> drop already correct via ordinary Rust ownership; "no long-lived sidecar pool" already satisfied by TASK-STMT-003's per-call spawn design; pending_bytes.rs already exceeds the requirement with a 30-min TTL + opportunistic pruning; the one named acceptance test already existed and passes. 393 passed (unchanged), same 4 pre-existing unrelated failures. |
| TASK-STMT-009 | STMT | done | 2026-07-15T13:00:00Z | The 5-permit concurrency cap itself already existed and was correct (FIFO wait, release after cleanup) but untested. Real gap: no ETA/batch-progress feature existed at all. Doc 30's named event (scan.progress) doesn't match Doc 19's catalog; added a purpose-specific statement_batch_progress event (matching this module's existing per-domain-event convention) rather than inventing TASK-RT-004's generic background-task-progress system early. New BatchProgressTracker (cumulative rolling average), wired through statements_upload (only for batches >10) and the queue dispatcher. 2 new tests. 395 passed, same 4 pre-existing unrelated failures. |
| TASK-STMT-010 | STMT | done | 2026-07-15T13:20:00Z | statements_retry_unprocessed existed but never actually reused stored passwords (always re-prompted). Root cause: try_stored_passwords("", ...) — instrument isn't known yet at password-resolution time, so the existing call site's empty-string scoping meant stored-password reuse silently never worked for ANY statement upload, not just retry. Added try_all_stored_passwords (cross-instrument scan), fixed both the real upload flow and retry. Added statements_list_unprocessed (3-bucket grouping) and statements_discard (delete + audit) from scratch — neither existed. 3 new tests. 398 passed, same 4 pre-existing unrelated failures. |

**Area 5 (Statement Processing) complete: all 10 tasks (TASK-STMT-001 through TASK-STMT-010) done.**
| TASK-TXN-001 | TXN | done | 2026-07-15T14:00:00Z | Orchestrator (run_extraction_ladder) already existed and stopped at first valid layer 1-4 correctly. Real bug: Layer 5 (LLM) was gated on per-bank template drift instead of RAM-eligibility (Doc 30's actual spec) — a brand-new bank could never reach it — and its sole call site hardcoded app_dir=None, so it was entirely dead in production. Threaded llm_eligible+real app_dir from AppHandle state through message_processor::process_message at both call sites (polling.rs, historical_scan.rs); drift detection demoted to a post-success side effect (pending-rule synthesis) instead of a precondition. Added missing confidence_score/parser_version fields (Doc 30's ExtractionResult definition); set confidence_score=0.7 for Layer 5 per FRS §6.3, left parser_version unpopulated (no versioning scheme defined anywhere). Rewrote 2 weak same-named tests to call the real function, added the 3rd missing named test via a capturing tracing layer. 398 passed, same 4 pre-existing unrelated failures. |
